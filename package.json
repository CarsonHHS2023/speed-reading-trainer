{
  "name": "speed-reading-trainer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check reader-api.js && node --check reader-model.js && node --check bookshelf.js && node --check app.js"
  }
}
